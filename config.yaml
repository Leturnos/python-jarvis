jarvis:
  threshold: 0.5
  cooldown_seconds: 2.5
  volume_multiplier: 1.0

integrations:
  warp:
    path: "${WARP_PATH}"

wakewords:
  hey_jarvis:
    action: warp
    commands:
      - 'cd ${PROJECT_PATH}'
      - 'gemini'
  
  fechar_tudo:
    action: system
    commands:
      - 'echo "Simulating close all..."'
