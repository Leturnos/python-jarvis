jarvis:
  threshold: 0.35
  cooldown_seconds: 2.5
  volume_multiplier: 2.0

integrations:
  warp:
    path: "${WARP_PATH}"

wakewords:
  hey_jarvis:
    action: warp
    commands:
      - 'cd ${PROJECT_PATH}'
      - 'gemini'
  
  jarvis_fechar_tudo:
    action: system
    commands:
      - 'echo "Simulating close all..."'
